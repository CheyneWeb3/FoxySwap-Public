Meet the Family metadata set

Images: images/1.png through images/10.png
Metadata: metadata/1.json through metadata/10.json

The image field currently uses the matching filename (e.g. 1.png). Replace with final IPFS/HTTPS image URI after upload if your minting system requires absolute URLs.
